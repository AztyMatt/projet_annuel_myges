apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-backups
  namespace: backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 30Gi
