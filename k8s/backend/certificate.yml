apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: myges-tls
  namespace: backend
spec:
  secretName: myges-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - __BACKEND_HOST__