apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: umami-tls
  namespace: observability
spec:
  secretName: umami-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - analytics.__FRONTEND_HOST__
